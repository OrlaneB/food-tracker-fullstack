// - meals for the day/meals cards list of ingredients and nutrients with values(custom hook to use in bar graphs comp)

import { createContext } from "react";

const mealsForOneDate = createContext();

export default mealsForOneDate;
//import to Bar graph meal cards
// add provider in homepage