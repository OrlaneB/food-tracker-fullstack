const express = require("express");
const router = express.Router();
const db = require("../model/helper");

// loop through incoming array of objects that is the list of ingredients
// const query = INSERT INTO ingredients ('name','numberAmount','mealId) 
        // VALUES('rice', '200', 23); //from req.data
        // POST request
        // await db()
//GET - ingredients 

//GET
router.get()

//POST with mealID